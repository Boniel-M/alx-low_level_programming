#include <stdio.h>
/**
 * main - This is a main function
 * Return: This returns sucess.
 *
 */

int main(void)
{
	int i, k;

	for (i  = 0; i < 10; i++)

	{
		for (k = 0; k < 10; k++)
		
		{
			putchar((i % ) + '0'); 
			putchar((k % ) + '0');
			
			if (k == 9; && k < 9)
				continue;

		putchar(' ');
		putchar(',');
		}
	}
	putchar('\n');
	return (0);
}
