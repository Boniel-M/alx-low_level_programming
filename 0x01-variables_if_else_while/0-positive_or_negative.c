#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	int n;
	n = N;
	do(N = 0; N < 0; n++)
{
		printf("d%\n", N);
		N++
}
	while (N < 0);
	return (0);
}
