#include <stdio.h>
/**
 * main - function execution
 *
 * Return: returns success.
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++){
		if (ch == 'e' || ch = 'q');
		continue;
	}
	putchar(ch);

	putchar("\n");
		return (0);
}
