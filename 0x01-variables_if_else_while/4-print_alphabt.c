#include <stdio.h>
/**
 * main function execution
 *
 * Return: returns success.
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	for (ch == 'e' || ch == 'q');
	continue;

		putchar(ch);
	putchar("ch\n")
		return (0);
}
