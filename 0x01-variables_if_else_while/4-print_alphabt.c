#include <stdio.h>
/**
 * main - function execution
 *
 * Return: returns success.
 */
int main(void)
{
	int char;

	for (char = 'a'; char <= 'z'; char++)
	for (char == 'e' || char = 'q');
	continue;

		putchar(ch);

	putchar("ch\n")
		return (0);
}
