#include <stdio.h>
/**
 * main - This is a main function.
 *
 * Return - returns value of main to (0) null.
 *
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	putchar ('\n');
	return (0);

}
