#include <stdio.h>
/**
 * main - This is a main function.
 * Return - returns value (0) success
 *
 */

int main(void)
{
	int ch;
	for( ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);

	putchar('\n');
	return (0);

}
