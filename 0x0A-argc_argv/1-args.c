#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * @argc: counts number of entries in the commandline
 * @argv: array of entries in the commandline
 *
 * Return: 0 success
 */
int main(int argc, char *argv[])
{
		printf("%d\n", argc - 1);
		return (0);
}
