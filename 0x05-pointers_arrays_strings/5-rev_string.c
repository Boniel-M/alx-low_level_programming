#include "main.h"
#include <stdio.h>
/**
 * rev_string - reverses a string
 * @s: string to be reversse
 * Return: void
 */
void rev_string(char *s)
{
	int i;
	int rev = 0;

	for (i = rev - 1; i >= 0; i++)
	{
		putchar(s[a]);
	}
}
